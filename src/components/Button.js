import React, { Component } from 'react';

class Button extends Component {
  render() {
    // You can use them as regular CSS styles
    return <button className="btn button-success">Search</button>
  }
}

export default Button;